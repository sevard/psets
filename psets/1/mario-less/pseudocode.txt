Ask user to input a number
Prompt user input while number is less than 1 or greater than 8

Loop number of times in height
    On each iteration
        spaces = (height - loop number)
        hashes = loop number
        print (spaces followed by hashes)